export const size = 64

export const gap = 4

export const sizeConfig = {
  size: 64,
  gap: 4,
  row: 4,
  column: 4
}

export enum Direction {
  Up = 'Up',
  Right = 'Right',
  Down = 'Down',
  Left = 'Left'
}
